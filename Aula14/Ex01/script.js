function contar(){
    var inicio = window.document.getElementById('inicio');
    var fim = window.document.getElementById('final');
    var passo = window.document.getElementById('passo');
    a = 1
    b = 8
    c = 1
    for (a; a <= b; a = a + c){
        res.innerHTML = `Contando: ${a}`;
    }
}
